\section{Benutzeroberfläche}
\tikzstyle{every node}=[draw=black,thick,anchor=west]
\tikzstyle{duplicate}=[draw=black,thick,dotted]
\tikzstyle{hidden}=[draw=gray!80,fill=gray!20]
\tikzstyle{hiddendupe}=[draw=gray!80,dotted,fill=gray!20]

Jeder Baum steht für ein Menü oder einen Zustand des Clients. 
Alle Zweige des Baumes stehen für Funktionen, die in diesem Zustand aufgerufen werden können.
Nach dem Aufrufen der Website befindet sich der Nutzer zunächst auf der Mainpage 
und kann von dort aus den Rest der Seite Navigieren. \newline
Ein gepunkteter Rand steht dabei für eine Weiterleitung in einen anderen Zustand.
Eine graue hinterlegung zeigt an, das der Nutzer keine dierekte Kontrolle über die entsprechende Funktion hat.


\vspace{0.5cm}
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
  \node {Mainpage}
    child { node {Enter Name /F0010/}}
    child{ node {Join Matchmaking /F0020/}
      child { node [duplicate]{Looking for Room}}
    }
    child [missing]{}
    child{ node {Join Room /F0030/}
      child { node [duplicate]{Looking for Room}}
    }
  ;
\end{tikzpicture}

\vspace{0.5cm}
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}] 
  \node {Looking for Room}
    child{ node [hiddendupe] {Pre-Match Phase}}
    child{ node [hiddendupe] {Mainpage}}
  ;
\end{tikzpicture}

\vspace{0.5cm}
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
  \node {Pre-Match Phase /F0050/}
    child { node {Room ID /F0120/}}
    child { node {Change Role /F0090/}}
    child { node {Vote for Quickstart /F0060/}}
    child { node [hidden] {Time up /F0060/}
      child { node [hiddendupe] {Generating /F0070/}}
    }
    child [missing] {}
    child [missing] {}
    child { node {Return to Mainpage /F0100/}
      child { node [duplicate] {Mainpage}}
    }
  ;
\end{tikzpicture}

\vspace{0.5cm}
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}] 
  \node {Generating}
    child{ node [hiddendupe] {Match Phase}}
  ;
\end{tikzpicture}

\vspace{0.5cm}
\begin{tikzpicture}[%
  grow via three points={one child at (0.5,-0.7) and
  two children at (0.5,-0.7) and (0.5,-1.4)},
  edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
  \node {Match Phase /F0080/}
    child { node {Spectate /F0090/}}
    child { node {Return to Mainpage /F0100/}
      child { node [duplicate] {Mainpage}}
    }
    child [missing] {}
    child { node [hidden] {End of Match}
      child { node [hiddendupe] {Pre-Match Phase}}
    }
  ;
\end{tikzpicture}
